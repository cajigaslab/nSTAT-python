version: 1
notes:
  - topic: nSTATPaperExamples
    file: notebooks/nSTATPaperExamples.ipynb
    source_matlab: nSTATPaperExamples.mlx
    fidelity_status: partial
    remaining_differences: Python uses standalone figshare-backed data access and generated gallery assets rather than MATLAB path-based setup, and several sections still rely on placeholder or tracker-only cells instead of full MATLAB-equivalent computations.
  - topic: TrialExamples
    file: notebooks/TrialExamples.ipynb
    source_matlab: TrialExamples.mlx
    fidelity_status: high_fidelity
    remaining_differences: The notebook now mirrors the MATLAB Trial workflow with executable object construction, masking, history extraction, and plotting; the closing analysis section uses one representative Python `Analysis` run instead of linking out to separate MATLAB help pages.
  - topic: AnalysisExamples
    file: notebooks/AnalysisExamples.ipynb
    source_matlab: AnalysisExamples.mlx
    fidelity_status: partial
    remaining_differences: Advanced MATLAB algorithm-selection branches and report plots remain lighter in Python, and the notebook still contains tracker-only visualization sections rather than a fully executable MATLAB-equivalent workflow.
  - topic: DecodingExample
    file: notebooks/DecodingExample.ipynb
    source_matlab: DecodingExample.mlx
    fidelity_status: high_fidelity
    remaining_differences: Workflow, model fitting, and decoded-stimulus figures now follow the MATLAB helpfile closely; exact traces still depend on stochastic simulation draws and Python plotting defaults.
  - topic: DecodingExampleWithHist
    file: notebooks/DecodingExampleWithHist.ipynb
    source_matlab: DecodingExampleWithHist.mlx
    fidelity_status: high_fidelity
    remaining_differences: The notebook now mirrors the MATLAB history-aware decoding workflow closely; exact stochastic trajectories and figure styling still vary slightly under Python execution.
  - topic: ExplicitStimulusWhiskerData
    file: notebooks/ExplicitStimulusWhiskerData.ipynb
    source_matlab: ExplicitStimulusWhiskerData.mlx
    fidelity_status: high_fidelity
    remaining_differences: The notebook now reproduces the dataset-backed lag search, stimulus-effect, and history-effect workflow with real figures; exact KS traces and coefficient values still vary modestly from MATLAB because the Python GLM backend and plotting defaults are different.
  - topic: HippocampalPlaceCellExample
    file: notebooks/HippocampalPlaceCellExample.ipynb
    source_matlab: HippocampalPlaceCellExample.mlx
    fidelity_status: high_fidelity
    remaining_differences: The notebook now reproduces the dataset-backed place-cell model-comparison and field-visualization workflow with real figures; the Python port still uses an approximate Zernike-like basis rather than the original MATLAB toolbox implementation.
  - topic: HybridFilterExample
    file: notebooks/HybridFilterExample.ipynb
    source_matlab: HybridFilterExample.mlx
    fidelity_status: high_fidelity
    remaining_differences: The notebook now reproduces the hybrid-filter simulation, single-run decoding, and averaged summary figures with real outputs; the Python port still uses the current hybrid-filter implementation instead of every MATLAB-specific reporting branch.
  - topic: PPSimExample
    file: notebooks/PPSimExample.ipynb
    source_matlab: PPSimExample.mlx
    fidelity_status: partial
    remaining_differences: The notebook now executes the full Python point-process simulation and analysis workflow without placeholders, but it still uses the native `CIFModel` path rather than the original MATLAB/Simulink recursive CIF model.
  - topic: ValidationDataSet
    file: notebooks/ValidationDataSet.ipynb
    source_matlab: ValidationDataSet.mlx
    fidelity_status: high_fidelity
    remaining_differences: The notebook now reproduces the constant-rate and piecewise-rate validation workflows with real `Trial`/`Analysis` objects and figure outputs; the Python port uses shorter deterministic simulations than MATLAB so the notebook remains stable in CI.
  - topic: StimulusDecode2D
    file: notebooks/StimulusDecode2D.ipynb
    source_matlab: StimulusDecode2D.mlx
    fidelity_status: high_fidelity
    remaining_differences: The notebook now reproduces the 2-D stimulus-decoding workflow with simulated receptive fields and decoded trajectories; the current Python decoder uses regression-based state recovery instead of MATLAB's symbolic-CIF nonlinear filter.
